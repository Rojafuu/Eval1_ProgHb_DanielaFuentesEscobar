import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eval1.proghb.danielafuentes',
  appName: 'eval1-proghb-danielafuentes',
  webDir: 'www'
};

export default config;
